services:
  mongodb:
    image: mongo:8
    container_name: ai-automation-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ai-instagram-automation
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
